What happens when you initialize a repository? Why do you need to do it?

When I initialize a repository git starts to track the files inside that repository. I need to do it
so that git can create the metadata to keep track of the files.

How is the staging area different from the working directory and the repository?
What value do you think it offers?

The staging area prepares the files to be commited to git repository.

How can you use the staging area to make sure you have one commit per logical
change?

The stating area works like a filter to the files that you want to commit, therefore,
you can use it to choose the files you want to commit.

What are some situations when branches would be helpful in keeping your history
organized? How would branches help?

Branches can help keeping production code working, not risking breaking it during development,
what could mess the process to fix a bug.

How do the diagrams help you visualize the branch structure?

Diagrams help visualizing the relation between the branches.

What is the result of merging two branches together? Why do we represent it in
the diagram the way we do?

The result of merging two branches is one branch with code from both branches.
We represent it the way we do because it helps identify when the merge happened.

What are the pros and cons of Git’s automatic merging vs. always doing merges
manually?
